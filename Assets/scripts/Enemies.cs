using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//IDamageable interface to cover getting damage

public class Enemies : MonoBehaviour
{
    /* MEMBER FIELD:
      
       hpMax;
       hpCurrent;
       velocity;

    */

    /* METHODS:
     
       movement(player.transform, velocity);
       ability();
       death();

    */

}

